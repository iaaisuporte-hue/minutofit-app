/** Workout builder tokens aligned with `src/styles/theme.css` (MinutoFit shell). */
export const WB = {
  text: "var(--white)",
  muted: "var(--muted)",
  muted2: "var(--muted2)",
  border: "var(--stroke)",
  borderStrong: "var(--stroke-strong)",
  card: "var(--card)",
  panel: "var(--panel)",
  primary: "var(--primary)",
  primarySoft: "var(--neon-primary-soft)",
  primaryBorder: "var(--neon-accent-border)",
  ctaText: "var(--neon-cta-text)",
  shadow: "var(--shadow)",
  danger: "var(--danger)",
  success: "var(--success)",
  bgDeep: "var(--black)",
} as const;
